apiVersion: v1
kind: Service
metadata:
  name: rabbit-cluster-ip
spec:
  type: ClusterIP
  ports:
    - port: 5432
      protocol: TCP
    - port: 15672
      protocol: TCP
  selector:
    app: rabbit
